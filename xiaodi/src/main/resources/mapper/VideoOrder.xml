<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.mapper.VideoOrderMapper">

    <select id="findByUidAndVidAndState" resultType="com.example.model.entity.VideoOrder">
        select * from video_order where  user_id =#{userId} and  video_id =#{videoId}  and state = #{state};
    </select>


    <!--	// 使用主键-->
    <insert id="saveOrder" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        INSERT INTO `video_order` (
	`out_trade_no`,
	`state`,
	`create_time`,
	`total_fee`,
	`video_id`,
	`video_title`,
	`video_img`,
	`user_id`
)
VALUES
	(
		#{outTradeNo},
		#{state},
		#{creatTime},
		#{totalFee},
		#{videoId},
		#{videoTitle},
		#{videoImg},
		#{userId}
	);
    </insert>

    <select id="listOrder" resultType="com.example.model.entity.VideoOrder">
		select * from video_order where user_id  = #{userId};
	</select>

</mapper>
